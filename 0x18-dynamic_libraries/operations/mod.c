#include <stdio.h>

/**
 * mod - This is a function that calculates the modulus
 * of two numbers
 * @a: first integer
 * @b: second integer
 *
 * Return: modulus
 */

int mod(int a, int b)
{
	return (a % b);
}
