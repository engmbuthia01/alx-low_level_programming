#include <stdio.h>

/**
 * mul - This is a function that multiplies two numbers
 * @a: first integer
 * @b: second integer
 *
 * Return: multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
