#include "main.h"

/**
 * _isdigit - check if the passed character is a digit or not
 * @c: int to be checked
 * Return: 1 if c is a digit, 0 if not
 */

int _isdigit(int c)
{

return (c >= '0' && c <= '9');
}

